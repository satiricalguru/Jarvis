# Core API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.7.0,<3.0.0

# HTTP client
httpx>=0.27.0,<1.0.0

# Environment / config
python-dotenv>=1.0.0,<2.0.0

# Telegram integration
python-telegram-bot>=21.0,<22.0

# TTS — lightweight fallback (always installed)
gTTS>=2.5.0,<3.0.0

# Hugging Face hub (model download + login)
huggingface_hub>=0.23.0,<1.0.0

# Pocket-TTS voice engine
pocket-tts>=0.1.0

# ML stack for audio post-processing (used lazily in voice_engine.py)
numpy>=1.26.0,<3.0.0
scipy>=1.13.0,<2.0.0
torch>=2.3.0
